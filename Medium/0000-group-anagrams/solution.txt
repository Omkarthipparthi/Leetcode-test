class Solution:
    def groupAnagrams(self, strs: List[str]) -> List[List[str]]:
        map = defaultdict(list)

        for s in strs:
            st = "".join(sorted(s))
            map[st].append(s)
        return list(map.values())